name: KubOS Build

on: [push]

jobs:
  action:
    name: Build KubOS image
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v3
        with:
          ref: 'buildroot-2022-02'

      - name: echo stuff
        run: |
          ls
          git status
        shell: 'bash'
